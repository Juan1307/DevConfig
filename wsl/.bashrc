# local commands
alias cls='clear'
alias ls-i='ls -l -A -h'
alias ..='cd ../..'
alias .='cd ..'
alias dev='bun run dev'
alias build='bun run build'
alias q='exit'

# keychain - git
eval ``keychain --eval --agents ssh id_rsa``